<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% if Helpers.is_logged_in?(session_hash)%>
  Hello <%= Helpers.current_user(session_hash).username%>
  Balance <%= Helper.current_user(session_hash).balance%>
  Log Out <a href='/logout'></a>
    <%else%>
  Please Log in<a href='/'></a>
  <%end%>
  </body>
</html>